import React, { Component } from 'react';
import { hot } from 'react-hot-loader';

class App extends Component {
  state = {};

  render() {
    return <div>React Mobiquity</div>;
  }
}

export default hot(module)(App);
